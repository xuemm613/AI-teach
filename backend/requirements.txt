# ---- Web 框架 ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0

# ---- 数据库 ----
SQLAlchemy==2.0.36
# MySQL 异步驱动（aiomysql 基于 PyMySQL）
aiomysql==0.2.0
PyMySQL==1.1.1
# MySQL 8 默认 caching_sha2_password 认证需要（RSA 公钥交换）
cryptography==44.0.0
# Milvus Lite：本地 .db 文件即向量数据库，无需 Docker。
# [milvus-lite] 为纯 Python 引擎（v3.x），支持 Windows / macOS / Linux；
# 若使用旧版 C++ 内核的 milvus-lite 仅支持 macOS/Linux x86_64。
pymilvus[milvus-lite]>=2.5.3

# ---- 认证（PBKDF2 + JWT，无编译依赖） ----
PyJWT==2.10.1

# ---- 大模型 API（OpenAI 兼容） ----
openai==1.59.6

# ---- 本地向量化（CPU 推理，首次需联网下载模型，之后可离线） ----
sentence-transformers==3.3.1

# ---- 文档解析 ----
pdfplumber==0.11.4
python-docx==1.1.2

# ---- 其他 ----
aiofiles==24.1.0
# Excel/CSV 批量导入题库与学生
openpyxl==3.1.5
aiofiles==24.1.0